%% Setup tables
clear;
load('tables.mat');

%% Lookup
d=-4e-3;
z=-4e-3;
r=0:0.001:0.1;
w = 20

[Gr] = g.lookup(r,z,d);
[Gfdm r2] = analy_sol_hs(abs(z+d));

figure (2);
clf(2);
subplot(211);
hold on
plot(r,real(Gr),'o')
plot(r2,real(Gfdm))
xlim([0,0.1])

subplot(212);
hold on
plot(r,imag(Gr),'o')
plot(r2,imag(Gfdm))
xlim([0,0.1])

